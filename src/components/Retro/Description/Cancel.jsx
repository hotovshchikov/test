import React from 'react';
import './Cancel.scss'

class Cancel extends React.Component {
  render() {
    return(
      <a className="button-red" id="close" href="#">Cancel</a>
    );
  }
}

export default Cancel;
